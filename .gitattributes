# Auto detect text files and perform LF normalization
* text=auto

# Ensure generated assets keep exact bytes (preserve SRI/hash)
docs/style/*.css -text
docs/style/**/*.css -text
docs/script/*.js  -text

# Ensure Git hooks use LF to avoid /usr/bin/env errors on Windows
.husky/* text eol=lf

